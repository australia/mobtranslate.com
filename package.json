{
  "name": "mobtranslate.com",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{js,jsx,md}\"",
    "prettier": "prettier -c ."
  },
  "devDependencies": {
    "prettier": "^3.1.0",
    "turbo": "^2.4.4"
  },
  "packageManager": "pnpm@9.15.0",
  "engines": {
    "node": ">=22",
    "pnpm": ">=9.15.0"
  }
}
