{
  "name": "mobtranslate.com",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "format": "prettier --write \"**/*.{js,jsx,md}\"",
    "prettier": "prettier -c ."
  },
  "devDependencies": {
    "prettier": "^3.1.0",
    "turbo": "^2.4.4"
  },
  "packageManager": "pnpm@7.15.0",
  "engines": {
    "node": ">=18"
  }
}
